  # === YAML — yamllint ===
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        args: [-c=.yamllint.yaml, -s]
        # Default config below if .yamllint.yaml is absent:
        #   extends: default
        #   rules:
        #     line-length: { max: 120, level: warning }
        #     truthy: { check-keys: false }
        #     document-start: disable
