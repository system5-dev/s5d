# .pre-commit-config.yaml — generated by code-quality skill.
# Re-run `bash ~/.agents/skills/code-quality/scripts/setup.sh` to regenerate.
# To install: `pip install pre-commit && pre-commit install`
# To run on all files: `pre-commit run --all-files`

default_install_hook_types: [pre-commit, commit-msg]
default_stages: [pre-commit]
fail_fast: false

repos:
